@model List<ProjectVM>

<div class="card shadow border-0 my-4">
    <div class="card-header bg-secondary bg-gradient m-lg-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Product List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">               
            </div>
            <div class="col-6 text-end">
                <a asp-controller="Project" asp-action="Upsert" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i>  Create New Project
                </a>
            </div>
        </div>
        <table id="tblData" class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        @Html.Label("Project Name")
                    </th>
                    <th>
                        @Html.Label("Project Manager")
                    </th>
                    <th>Admin</th>
                </tr>
            </thead>
        </table>
    </div>
</div>
@section Scripts{
    <script src="~/js/project.js"></script>
}

@* @model IEnumerable<SolairxExample.Model.Project>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-4"><h1>Projects</h1></div>
    <div class="col-4"></div>
    <div class="col-4"><a asp-action="Create" class="btn btn-primary">Create New</a></div>

</div>
<br />
<br />
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.Label("Project Name")
            </th>
            <th>
                @Html.Label("Project Manager")
            </th>
            <th>Admin</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <input hidden value="@item.ProjectId" />
            <tr>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ProjectId">@item.ProjectName</a>
                </td>
                <td>
                    @Html.Raw(item.ProjectManagerNavigation.LastName + ", " + item.ProjectManagerNavigation.FirstName)
                </td>
                <td>
                    <a asp-action="Delete" asp-route-id="@item.ProjectId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table> *@
